# DEVLOG — 开发日志

## 2026-09-02 v1.0.0（单日完成）

1. **数据源验证（先行）**：会话早段已实测 Yahoo 不可达、Stooq JS 挑战、东财全三标的可用；
   本次复验并补齐新浪/腾讯结构与历史深度实测，产出 `tools/verify_data_sources.py` 与验证报告。
   关键发现：`100.NDX100` 才是纳斯达克100；黄金现货=`122.XAU`（8918 根）。
2. **后端**：按需求 §14 分层实现。数据清洗（NaN/Inf/非正/重复日期/排序）、
   完整交易日口径（盘中不进 SMA800）、两模型纯函数、45 个单测。
3. **联调排障（重要）**：
   - 东财 HTTPS 主域断连 → 改 HTTP + 数字子域镜像多端点；
   - requests 读 Windows 注册表系统代理导致 ProxyError → 直连优先（trust_env=False）+ 代理兜底；
   - 东财 WAF 按 TLS/请求头指纹拦截脚本 → 浏览器级请求头 + Connection: close 解决；
   - 重写 fetch 层时丢失"单源异常继续回退"逻辑导致 502 → 修复并补 3 个回退链单测；
   - 东财限流期间验证了回退链真实接管（NDX→新浪、000905→腾讯、XAU→新浪）。
4. **前端**：Next.js 15.4.6 → 15.5.25（CVE-2025-66478 修补版）；build + 生产模式运行；
   agent-browser 实测渲染、输入金额、截图取证。
5. **端到端验收**：需求 §19 十四项全部通过（详见 README 测试与验证章节）。

## 技术要点备忘

- pytest 中文输出需 `PYTHONUTF8=1`。
- agent-browser 需先 `agent-browser install` 下载 Chromium；Windows 下守护进程偶发 SIGTERM，
  长命令链 + 输出重定向到文件更稳。

## 2026-09-02 18:20 补充

6. **验收报告**：`docs/验收报告.md` 对照需求 §19 十四项逐条留证（两轮实测、金额两例、截图清单）。
7. **晚间复验**：18:17 再次实测（NDX 21:49 ET 盘后价、中证500 今日收盘、黄金 18:17 实时），
   pytest 45/45；非整数金额 1234.56 分摊合计精确等于输入（397.56+441.32+395.68）。
8. **桌面启动器**：COM（WScript.Shell）创建 .lnk 被安全策略拦截 → 改为桌面 .bat 包装器
   （内含项目绝对路径，不依赖当前目录）。
