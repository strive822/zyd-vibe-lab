# DECISIONS — 技术决策

### 数据源采用"东财主源 + 按资产单回退源"（2026-09-02）
- 决策：NDX=东财 100.NDX100（回退新浪 .NDX）；000905=东财 1.000905（回退腾讯 sh000905）；
  XAU=东财 122.XAU（回退新浪 hf_XAU）
- 选择原因：需求 §3 允许"最少数据源组合"；当日实测东财存在秒级瞬断与指纹拦截，
  单源不可靠；三套适配器在限流期间被实测接管过全部流量
- 放弃方案：yfinance（本机不可达）、Stooq（JS 挑战）、腾讯作 NDX 回退（无深历史）、
  多源负载均衡（超出第一版需要）
- 长期影响：新增资产只需在 config.py 加 SourceSpec 与对应 fetcher

### 60 秒真实数据内存缓存 + 整流重试（2026-09-02）
- 决策：同一资产 60 秒内复用上次真实抓取结果；全流程失败后等 2 秒重试一次
- 选择原因：免费源频控导致偶发 502；页面打开/刷新仍走完整抓取流程，不违反需求 §12；
  只缓存真实数据，不违反"不伪造数据"
- 放弃方案：无缓存（502 率高）、更长的 TTL（行情新鲜度下降）
- 长期影响：如需调整口径，改 `market_data._CACHE_TTL_SECONDS`

### 金额分摊放在前端本地计算（2026-09-02）
- 决策：后端 API 只返回权重；输入金额后由前端按最大余额法分摊到分
- 选择原因：需求 §12 要求"修改金额不重新请求行情"，前端本地计算天然满足且零延迟
- 放弃方案：后端提供金额计算接口（会诱导重新请求）
- 长期影响：TS 与 Python 各有一份分摊逻辑（仅前端那份参与实际显示），修改时需两边同步

### 项目代码布局采用 backend/ + frontend/（偏离工作区 src/ 规范）（2026-09-02）
- 决策：代码目录按用户需求文档 §14 的 backend/frontend 布局；工作区 8 文件标准照常执行
- 选择原因：需求文档明确给出目录参考，用户需求优先于工作区默认；
  SOP_Standards 允许"特殊情况在项目中覆盖并记录原因"
- 放弃方案：backend/frontend 塞进 src/ 下（徒增层级、与需求文档不一致）
- 长期影响：本项目内找代码以需求文档结构为准
