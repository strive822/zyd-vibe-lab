# 当前候选与验收状态

更新：2026-09-25。

## 当前工程候选

- 文件：`bin\QuotaWidget.staged-165-30642.exe`
- SHA-256：`69acfbdf781d7a784639a6b3672d954de90a4c2980fbb168b2c58369c14feba8`
- 独立复审实际运行 `build.bat --test-only`：**402 条断言全部通过**。
- 原 P1“顶部打开设置时重叠”与 P2“开机自启写入失败无反馈”均已关闭；当前已审范围无未解决 P0 / P1 / P2。
- 当前候选未替换 `bin\QuotaWidget.exe`；旧版本仍在运行不代表已切换到该候选。

此前用户已认可像素视觉方向及旧候选的设置滚动。该认可保留为设计依据；新工程候选的物理操作与目视验收仍待确认，真实 Provider 连通性本次未验证。

## 验证范围

| 项目 | 当前结论 |
| --- | --- |
| 配置、解析、认证、调度与 UI 回归 | 402 条断言通过，映射见 TEST_PLAN.md |
| 顶部开窗、内容变化与短工作区 | 真实 HWND 和按钮边界测试通过 |
| 开机自启失败与成功重试 | 注入测试通过，未故意修改真实注册表权限 |
| 当前候选实际鼠标菜单、保存取消、托盘操作 | 待实机确认 |
| 当前候选实际系统 DPI、跨屏、高对比、读屏 | 本次未重新验证 |
| 当前真实 Codex、智谱、DeepSeek 接口 | 本次未验证，需分别记录 |
| 正式程序替换 | 未执行 |

## 实机确认方法

在 **Windows 命令提示符**执行：

```bat
cd /d "E:\pi always\1a\redesign"
bin\QuotaWidget.staged-165-30642.exe --preview --preview-scenario=normal
```

1. 将主窗拖到工作区顶部，从实际菜单打开设置；检查设置在上、两窗不交叠、保存与取消可达。
2. 取消后确认主窗恢复位置；修改合成名称后保存，确认数据应用。长内容能滚动，右侧指示线跟随。
3. 检查托盘隐藏/恢复及退出，记录实际系统缩放和显示器环境。

预览只使用合成数据，不能验证正式配置持久化或真实接口。发现问题时记录候选哈希、环境、操作步骤和实际结果，按需生成新的诊断材料。

## 目录维护

用户要求目录只保留后续运行和维护有用的文件，因此旧截图、逐轮评审素材、构建日志、一次性诊断工具、过时 EXE 和早期实现已清理。当前规范、自动化回归、候选身份及上述验收边界继续保留；早期配置放在 `local-backup/` 供手动恢复。后续截图按需生成，避免长期堆积。
